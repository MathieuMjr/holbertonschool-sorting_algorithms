#include "sort.h"

/**
 * insertion_sort_list - sort data in a 
 * doubly linked list
 * @list: pointer to the head of the list
 *
 * Return:
 */
void insertion_sort_list(listint_t **list)
{
	listint_t *reading_head = *list;
	listint_t *next = reading_head->next;

	while (reading_head != NULL)
	{
		if (reading_head->n > )
		reading_head = reading_head->next;
	}
}
